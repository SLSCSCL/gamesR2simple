event_errs = {
    "overridden_event": "{} events must not already exist as a {} event",
    "mouse_e_callback_arg_undef": "Mouse{} events callbacks require 1 " +
        "positional argument for the mouse pos"
}
